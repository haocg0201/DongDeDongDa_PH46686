<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyFootballApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

    </style>
    <style name="CustomSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- <item name="queryBackground">@android:color/white</item>  -->
        <item name="searchHintIcon">@drawable/ic_search</item> <!-- Đặt icon search nếu chưa có -->
        <item name="android:textColor">#000000</item> <!-- Màu chữ khi nhập -->
        <item name="android:textColorHint">#757575</item> <!-- Màu chữ gợi ý -->
    </style>
</resources>